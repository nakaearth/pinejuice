# frozen_string_literal: true

{
  encrypted_user_id: @current_user[:encrypted_user_id],
  name: @current_user[:user].name,
  nickname: @current_user[:user].nickname,
  image_url: @current_user[:user].image_url
}
