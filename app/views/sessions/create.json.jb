{
  encrypted_user_id: @current_user_json[:encrypted_user_id],
  name: @current_user_json[:user].name,
  nickname: @current_user_json[:user].nickname,
  image_url: @current_user_json[:user].image_url,
}
